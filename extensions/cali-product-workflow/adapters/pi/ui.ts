/**
 * Pi UI Adapter - Simplified
 * 
 * No longer uses Pi TUI overlays.
 * Uses generic adapter for cross-CLI compatibility.
 * 
 * @deprecated - Use generic adapter instead
 */

export function initPiUI(): void {
  // No-op - using generic adapter
}